package storage.memory;

import storage.AbstractStorageTest;
import com.urise.storage.memory.ListStorage;


public class ListStorageTest extends AbstractStorageTest {

    public ListStorageTest() {
        super(new ListStorage());
    }
}
